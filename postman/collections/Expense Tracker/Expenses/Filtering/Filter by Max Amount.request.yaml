$kind: http-request
name: Filter by Max Amount
method: GET
url: '{{baseUrl}}/expenses'
order: 1040
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
params:
  - key: max_amount
    value: '1000'
    enabled: true
