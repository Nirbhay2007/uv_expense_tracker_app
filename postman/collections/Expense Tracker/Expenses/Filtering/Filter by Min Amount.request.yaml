$kind: http-request
name: Filter by Min Amount
method: GET
url: '{{baseUrl}}/expenses'
order: 1030
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
params:
  - key: min_amount
    value: '100'
    enabled: true
