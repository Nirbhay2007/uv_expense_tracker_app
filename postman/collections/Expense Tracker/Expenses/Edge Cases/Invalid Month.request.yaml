$kind: http-request
name: Invalid Month
method: GET
url: '{{baseUrl}}/expenses/monthly-summary'
order: 1150
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
params:
  - key: year
    value: '2026'
    enabled: true
  - key: month
    value: '13'
    enabled: true
